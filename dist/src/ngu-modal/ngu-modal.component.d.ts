import { NguModal } from './ngu-modal.interface';
import { Subscription } from 'rxjs/Subscription';
import { NguModalService } from './ngu-modal.service';
import { OnInit, OnDestroy, ElementRef, Renderer, EventEmitter } from '@angular/core';
export declare class NguModalComponent implements OnInit, OnDestroy {
    private modal;
    private el;
    private renderer;
    modalDiv2: any;
    modalDiv1: any;
    overlay: any;
    modalDiv: any;
    modalSub: Subscription;
    openModals: any;
    MIndex: NguModal;
    beforestart: EventEmitter<any>;
    afterstart: EventEmitter<any>;
    beforeend: EventEmitter<any>;
    afterend: EventEmitter<any>;
    modalMain1: ElementRef;
    modalInner: ElementRef;
    constructor(modal: NguModalService, el: ElementRef, renderer: Renderer);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private generateID();
    close(): void;
    backdrop(): void;
    private toggleModal(val);
    confirm(m: number, message?: string): Promise<boolean>;
}
